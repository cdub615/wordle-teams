# Task 17 — the written checklist walk

`wt-ksh.8.40`, which is `wt-ksh.8`'s acceptance criterion.

**The rule this document is held to:** every difference resolves to a **section 7a
divergence** or a **Beads bug**. Nothing stays here as a note. If a line below
has no resolution, Task 17 is not done.

---

## Before you start

- [x] **Nothing on port 3000.** `ss -ltnp | grep :3000`. A leftover `vite dev`
      serves whatever code it last loaded, and every e2e result taken against it
      is meaningless (`wordle-teams-9mjm` — a two-day-old server invalidated a
      week of runs).
- [x] **Do not inspect a fetched page with `grep`.** Every SSR document this app
      serves contains NUL bytes — TanStack serialises route ids with a trailing
      NUL — so `grep` treats the response as binary and reports **no matches,
      with no error**. It looks exactly like the content being absent, and it
      already produced one false conclusion (that the landing page was
      client-rendered and invisible to crawlers, when it was fully
      server-rendered). Use `grep -a`, `tr -d` the NUL, or a real parser.

      *This document could not be written into the repo on the first attempt
      because the paragraph above contained a literal NUL. The trap is live.*

---

## Part 1 — the mechanical half, and it is fully resolved

`node scripts/parity-routes.mjs`, run **2026-09-03**. 15 routes: 1
missing-on-prod, 12 differs, 1 expected, 1 match.

**Every field it flags now has a resolution.** Four section 7a rows were written
during this task because the audit found differences the table did not explain —
which is that section's own rule working as intended.

| Difference the script reports | Resolution |
| --- | --- |
| `cacheControl` on 8 routes | 7a **54** (new) — static documents cached at the edge for a day |
| `canonical` on 7 routes | 7a **55** (new) — per-route canonical; production declares none |
| `og:url` on 8 routes | 7a **55** (new) — same mechanism; `/home` points at the apex |
| `og:image` on 8 routes | 7a **56** (new) — absolute, fingerprint dropped |
| `contentType` on `/robots.txt`, `/sitemap.xml` | 7a **57** (new) — explicit `charset=utf-8` |
| `/about` `title` | 7a **43** — `/about` sets its own title |
| `/me` `location` `/login` to `/app` | 7a **40** — the dashboard moved to `/app` |
| `/app` 404 to 307 | 7a **40** — same |
| `/branding` 307 to 404 | 7a **42** — dropped, no v2 equivalent |
| `/me`, `/complete-profile` redirect headers | `wordle-teams-d2oc` — redirects carry no `Cache-Control` |
| `/opengraph-image.png` | match |

---

## Part 2 — the interactive walk

**Status 2026-09-03:** everything below is verified except **maintenance mode**,
which needs a Cloudflare dashboard flip and is scheduled for 2026-09-04. The PWA
start_url item is resolved — see its entry.

Every row is a **prod screen** with a tick against **beta**. Where beta is
expected to differ, the expectation is named, so a tick means "matched the
expectation" rather than "looked fine".

### Sign-in

- [x] OTP by email — code arrives, signs in, lands on `/app`
- [x] Google
- [x] Microsoft
- [x] GitHub
- [x] Discord
- [x] **A provider returning a different address makes a different account.**
      Expected, not a bug: `convex/access.ts` resolves a session to a player by
      email alone. The account menu now shows which address you are under
      (7a 53), which is the only way to diagnose it.
- [x] Sign out — `selectedTeam` cleared from localStorage, theme kept, and the
      react-query cache cleared so the next account sees no stale data

### Dashboard

- [x] Scores table — rows, day columns, score column
- [x] Month dropdown. **Newest first** on beta, oldest first on prod (7a 48)
- [x] Team picker; **"Upgrade for more" replaces "New Team" at the cap**
- [x] TeamBoards carousel — scroll-snap, not embla (7a 31)
- [x] Day arrows cross months (7a 51); day picker reaches every month the
      dropdown offers (7a 50)
- [x] A concealed board shows **no letters at all** (7a 34)
- [x] Board entry — submit, edit, duplicate submit does not double-write (7a 2)
- [x] Skeletons on team/month switch, not a blank grid (7a 49)
- [x] Celebration dialog — **names the winner**, not the reader (7a 35); CSS
      confetti; respects `prefers-reduced-motion` (7a 36, 37); dismissing it
      sticks across a reload (7a 38)

### Teams

- [x] Create
- [x] Rename
- [x] Invite an address with no account — "Invite sent"
- [x] Invite someone already on the team — says so, dialog stays open (7a 9)
- [x] Invite a non-pro invitee at the cap — parked, with the "maximum number of
      teams" toast (7a 8)
- [x] Pending invites visible to the owner, and cancellable (7a 6)
- [x] Remove a member — past winners recomputed (7a 5)
- [x] Leave a team (7a 10)
- [x] Delete a team
- [x] **A non-owner cannot rename, invite or remove** — enforced server-side on
      beta where v1's RLS lets any member write (7a 4)

### Billing

- [x] Upgrade CTA to Polar checkout, returns pro
- [x] Billing portal opens for a subscriber
- [x] **Billing is shown to every signed-in player** (7a 39) and the portal
      creates a customer on demand for a comped/never-subscribed account
      (7a 47) — so it opens rather than dead-ending

### Settings

- [x] Time zone — including one **outside the curated 27**, which must display
      truthfully and not be silently replaced
- [x] Reminder time
- [x] Email reminders toggle
- [x] Push toggle — absent where no VAPID key is configured
- [x] Install Guide tab
- [x] **"Signed in as" with the display name above it** (7a 53)

### Error and edge states

- [x] `/login-error`
- [x] A route that does not exist — 404
- [x] Offline — the static offline page, and **no other user's document served
      from Cache Storage** (7a 18)

### Maintenance mode

- [ ] Flip `MAINTENANCE` to `true` in the Cloudflare dashboard
- [ ] `/app`, `/me`, `/complete-profile`, `/login` and `/` all 307 to
      `/maintenance`
- [ ] `/home`, `/about`, `/privacy`, `/terms` **stay up** — deliberate
- [ ] **`/` needs a cache purge, and this is now load-bearing.** `/` is the only
      path in both `GATED_PATHS` and `STATIC_DOCUMENTS`, and since
      `wordle-teams-fqeq` it is genuinely cached at the edge — a cached 200
      outlives the flag and the Worker never sees those requests. Purge the
      apex, or redeploy (the cache key carries `CF_VERSION_METADATA.id`, so a
      new version misses everything). `wt-ksh.8.52`.
- [ ] Flip it back to `false` and confirm the site returns

### PWA

- [ ] Install from beta; launches standalone
- [ ] Icons and name correct
- [x] **An install carrying the OLD `/me` start_url still lands correctly** —
      resolved 2026-09-03, and the risk turned out to be structurally covered.

      **The manifests, measured:** production ships `start_url: "/me"`, beta
      ships `start_url: "/app"`, and **neither declares `scope`**. With no
      `scope`, the default is the `start_url` with its last segment removed —
      `/` in both cases. So `/me` and `/app` are both in scope, and the redirect
      **cannot** kick a standalone window out to the browser, which was the
      actual failure mode worth worrying about. `/me` answers `307 -> /app` on
      beta, confirmed by request.

      **WHAT STILL CANNOT BE TESTED BEFORE CUTOVER, and the preparation that
      makes it testable ON the day:** no beta install can carry `/me`, because
      beta's manifest says `/app`. The only artifact that reproduces this is an
      install made from **production** — so **install the PWA from
      wordleteams.com now and keep it**. On cutover day, launching that install
      IS the case, with nothing to simulate.

      **`/me` MUST NEVER BE REMOVED.** Every pre-cutover install depends on it
      permanently, and the route is the only thing standing between those users
      and a dead app icon. See `wordle-teams-cog5`: the route file calls the
      redirect "PERMANENT" while the wire says 307. **307 is the safer of the
      two here** — nothing is cached, so a browser re-asks on every launch,
      where a 301 would be cached by exactly this population and is irreversible
      in every browser that has seen it. The wording is what should change, not
      the status.
